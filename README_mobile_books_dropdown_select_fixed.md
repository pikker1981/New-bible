# Mobile BOOKS selectable row fix

- Restores explicit selectable controls in the mobile BOOKS dropdown list.
- Keeps each row as a clickable button with a visible 선택 / 선택됨 pill.
- Preserves global search, notes, glossary spacing, and highlight behavior.

Apply by replacing:

```txt
app.js
index.html
mobile_books_dropdown_select_fixed.css
data/manifest.json
```
